services:
  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
    container_name: meajudaai-frontend
    environment:
      NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-http://host.docker.internal:5231}
      PORT: 3000
    ports:
      - "3000:3000"
